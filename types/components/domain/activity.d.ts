export interface ActivityType {
  title: string;
  collected: number;
  target: target;
  image: string;
  slug: string;
}

export interface ComponentDomainActivityCardType {
  title: string;
  target: number;
  collected: number;
  image: string;
  link: string;
}